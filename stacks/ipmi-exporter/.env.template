# IPMI Exporter Configuration
# No environment variables needed - credentials passed via Prometheus config

# IPMI BMC Access (for reference - configure in Prometheus)
# IPMI_HOST=192.168.1.11       # narsis IPMI IP (check router DHCP leases)
# IPMI_USER=ADMIN              # IPMI username (after reset)
# IPMI_PASSWORD=***            # IPMI password (set during narsis setup)
