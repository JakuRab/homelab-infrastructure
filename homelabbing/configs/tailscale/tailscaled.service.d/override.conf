[Unit]
# Ensure tailscaled starts after the network is truly online
Wants=network-online.target
After=network-online.target

# Avoid systemd start-limit lockouts on transient failures
StartLimitIntervalSec=0

[Service]
# Always restart tailscaled if it exits for any reason
Restart=always
RestartSec=5s
