# Smartctl Exporter - Disk Health Monitoring
# No additional configuration needed

# Settings (configured in docker-compose.yml):
# SMARTCTL_REFRESH_INTERVAL=60      # How often to poll SMART data (seconds)
# SMARTCTL_SCAN_ON_START=true       # Scan for devices at startup
