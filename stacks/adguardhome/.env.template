# AdGuard Home Configuration
# No secrets required - configured via web UI after deployment

# Optional: Initial admin password (set during first-run wizard)
# ADGUARD_ADMIN_PASSWORD=your_secure_password
