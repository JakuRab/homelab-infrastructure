# ==========================================
# User/Group Configuration
# ==========================================
# Run 'id' command on narsis to get your user's PUID and PGID
# Example: id athires
#   uid=1000(athires) gid=1000(athires) groups=...
PUID=1000
PGID=1000

# ==========================================
# General Settings
# ==========================================
# Timezone (must match your location)
TZ=Europe/Warsaw

# ==========================================
# Jellyfin Configuration
# ==========================================
# Public URL for Jellyfin (used for DLNA announcements and remote access)
# Example: https://jellyfin.rabalski.eu or https://media.rabalski.eu
JELLYFIN_URL=https://media.rabalski.eu

# ==========================================
# Navidrome Configuration
# ==========================================
# How often to scan for new music files (cron format)
# Examples:
#   @every 1h = every hour
#   @every 24h = once a day
#   1 2 * * * = 2:01 AM daily
NAVIDROME_SCAN_SCHEDULE=@every 1h

# Log level: error, warn, info, debug, trace
NAVIDROME_LOG_LEVEL=info

# Session timeout (how long users stay logged in)
# Examples: 24h, 7d, 720h (30 days)
NAVIDROME_SESSION_TIMEOUT=24h

# Base URL if behind reverse proxy (leave empty if using root domain)
# Example: /music if accessing via https://rabalski.eu/music
# Leave empty for subdomain: https://music.rabalski.eu
NAVIDROME_BASE_URL=
